import CssDoc from './components/CssDoc.vue'
import HelloWorld from './components/HelloWorld.vue'
import LiveExample from './components/LiveExample.vue'

| Why | What |
:--- | :---
| Cause | I'm a table demonstrating Github Markdown. |
| That's great! | I know. |

This page demonstrates how to extract and display CSS documentation in MDX files.

It also demonstrates that embedding Vue components in mdx works.  Here's a counter button:

<HelloWorld />

## Live Code Examples

Here's a simple live example you can edit:

<LiveExample
  title="Basic HTML Example"
  dataScope={{ Date }}
  code={`<template>
  <div>
    <h3>Hello from Vue Live!</h3>
    <p>Current time: {{ new Date().toLocaleTimeString() }}</p>
    <button @click="count++" class="btn btn-primary">
      Clicked {{ count }} times
    </button>
  </div>
</template>

<script>
export default {
  data() {
    return {
      count: 0
    }
  }
}
</script>`}
/>

## Overview

This is the main design system documentation page. You can navigate to specific sections using the header links or search functionality.

## Style Guide

You can extract documentation from any CSS file by using the `<CssDoc>` component:

```jsx
<CssDoc filePath="path/to/your/css/file.css" title="Optional Title" />
```

The component will automatically parse JSDoc-style comments from your CSS files and display them in a beautiful format.

## Supported Comment Formats

- `@param` - Document parameters or modifiers
- `@example` - Show usage examples
- `@since` - Version information
- `@deprecated` - Mark as deprecated
